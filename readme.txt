1.找出完整三元组和缺失三元组，保存在两个excel
一共61267个专家("#"专家不算)，其中3257个专家（"#"专家不算）的单位未知，占比5.32%。从27432行开始出现缺失。
2.有些专家其实是个“单位”，这在图谱中是合理的，但现在不合理，将其剔除。
3.有些关键词搜索结果没有50个，在selenium操作找下一页时发生错误，需改正。
4.网页编码不同，需要根据编码爬取网页，否则会出现中文乱码。
5.对于空URL，直接返回，不要捕获异常，很慢。
6.如果小于15页，next-page按钮查找错误。应该优先使用class，而不是xpath
7.文件夹越来越大，项目启动时长过长，为28min52s。
8.有些单位其实是子单位，或包含不合法字符。在筛选有效网页时应该计算相似度，而不是看是否完全出现。
9.URL文件名称最后有空格，而在建立文件夹时系统自动忽略空格，导致找不到文件夹。修正。
10.URL文件名中专家未知，用“#”代替了，这属于无效三元组，剔除。
11.碰到不合法三元组，直接修改知识图谱，包括：知识图谱，构建excel，comexcel，文件夹。
12.程序读取文件夹时，10在2的前边，需要改为2进制。最高61267，使用16位2进制保存。
13.第59，193，292，424，719，905，909，932，936，943，1023，个HTML文件应该有一定问题。
14.专家或单位的名称中包含空格，应该去除。从知识图谱开始去除。
15.单位名称中可能有括号，这是正常的，但有的是中文括号，有的是英文，需要注意，否则可能无法识别。
16.提取尾实体时，先找到所有的机构，然后找到机构的上下文，根据模式从上下文中确定争取的机构
17.有些专家名称是单位、音译词、特殊含义，这些都需要剔除。只能识别一般的人名，英文名也可以。
18.两种方法抽取尾实体。第一是从训练集的content中获取模式，在测试集的content中抽取。第二是在测试集的html或者content中进行NER。
19.keyword只有专家名称不行，因为有重名等因素。应该是名称+智慧城市。
20.有些缺失单位的专家是有联系的，可能是一家公司的，需要考虑进去。将这几个关键词的文件放到一块处理？
21.一个关键词好像不够，如“赵鲁军 智慧城市”没有有效网页，但是“赵鲁军 枣庄”就有很多有效网页，“枣庄”是从“赵义湘 智慧城市”找到的一个关键词。现在看来同一组关键词之间的联系可以深度挖掘，否则很难找到有效信息。
22.从网页中提取机构名，和地名。而不只是机构名。
23.接下来要进行分词与NER了。在这一步得到一些信息后，再用原始文本提取模式。

提取网页内容（应该建立比较复杂精细的模式，便于后续的处理）：
1.<strong>,<b>,<p>替换为“无”,其他的替换为空格。
2.目前发现3,4,6,11,12,13,14,15的提取效果不好。后续观察。

一些想法的改变：
1.不再严格要求单位名称的出现，只要是单位且相似度大于一定阈值就可以。
2.更改名称，建立相近词库？？


目前在做的是FrontHalf中，extractPattern模块的，Test.class和FindExpertMaxScoreEnter_nlp.class。
要找到一个datFile中，与真实结果最接近的几个term。


1. Script中是一些脚本类，不太可能被复用，也不严格属于哪个模块。

2.一个表中存36万行数据，那么一个普通的select * from table where id = "xx"需要近20s的时间，必须分表。

3.因为word“二〇〇五”报错 Duplicate entry '二〇〇五' for key 'sgns_sougou_bigram_yun.PRIMARY',因此删去了几条数据，
在327641行, 248741

4.各个字母开头的word个数
    非汉字：(只包含中英文标点符号和数字，一些特殊字符略过)
    other:39584
    a:4822, o:568, e:1255, i:0, u:0, v:0.  total:6645
    b:14649, c:17208, d:17531, f:

5.如何评价一个myterm是所求结果的置信度？一个term对应一个评分，可用回归模型。
训练集：根据相似度为每个term打分。
如何定义相似度？编辑距离、语义距离、自定义距离（比如是层级关系？取最高值？）
由于word是一个机构名，也就是一个短语，如何通过词向量表示短语向量？这也是比较新的研究，值得说。最长前缀匹配，限制短语的关键词个数的最大值。
写代码时有两个选择，一是用学术的方法(KCNN等)，二是直接用词向量的均值，或者补零等特殊值处理（补零要考虑零值的语义）



